import { Typography, Box, Container, Card, Button, Grid } from "@mui/material";

import type { NextPage } from "next";
import { Layout, Section } from "../../../components/Layout";

const TvProgram: NextPage = () => {
  return (
    <Layout title="TV Program" wider>
      <Section>
        a
      </Section>
    </Layout>
  );
};

export default TvProgram;
